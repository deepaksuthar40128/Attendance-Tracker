<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="../css/profHome.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            overflow-x: scroll;
            padding: 20px;
            background-color: #f1f1f1;
        }

        .card {
            flex: 0 0 auto;
            width: 300px;
            margin-right: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
            padding: 20px;
        }

        .card img {
            width: 100%;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .card h3 {
            font-size: 18px;
            margin-bottom: 10px;
        }

        .card p {
            margin-bottom: 10px;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
        }

        .button-container button {
            flex: 1;
            padding: 10px;
            margin-right: 5px;
            border-radius: 3px;
            cursor: pointer;
            border: none;
            color: #fff;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .button-container button.present {
            background-color: #4caf50;
        }

        .button-container button.absent {
            background-color: #f44336;
        }

        .btns {
            max-height: none;
        }
    </style>
</head>

<body>
    <div class="phone">
        <div class="topnav">

            <h3 class="text">Attendence <br> Traker</h3>
            <div class="ricon">
                <i class="fa fa-bars"></i>
            </div>
            <div class="licon">
                <i class="bi bi-person-fill"></i>
                <i class="bi bi-search"></i>
            </div>
            <% if( error !="" ){ %>
                <div class="error" role="alert">
                    <strong> Error </strong>
                    <%= error %>
                        <button type="button" onclick="document.getElementsByClassName('error')[0].remove()"
                            class="btn-close">x</button>
                </div>
                <% } %>
                    <% if( error_messages !="" ){ %>
                        <div class="error" role="alert">
                            <strong> Error </strong>
                            <%= error_messages %>
                                <button type="button" onclick="document.getElementsByClassName('error')[0].remove()"
                                    class="btn-close">x</button>
                        </div>
                        <% } %>
        </div>
    </div>
    <div class="review">
        <h2>Attendence of <%= courseName %>
        </h2>
        <h2>Date: <%=date%>
        </h2>
        <div style="    display: inline-block;
    position: absolute;
    right: 10px;
    top: 100px;">
            <button onclick="subData()" >Submit Attendence</button>
        </div>
    </div> 
    <input type="text" hidden id="courseId" value="<%=course_id%>">

    <div class="btns">
        <div class="container" id="functional">

            <!-- Add more cards here -->
        </div>
    </div>
    
    <div class="foot">
        <i class="bi bi-house-door"></i>
        <i class="bi bi-journal-text"></i>
        <i class="bi bi-person"></i>
        <i class="bi bi-bell"></i>
    </div>
    </div>
    <script src="../js/takeAttandance.js"></script>
</body>

</html>